#include<stdio.h>
#include<stdlib.h>

typedef int boolean;

void push (void);
void traverse(void);
void exit(void);
int pop(void);
int peek(void);
int size(void);
boolean isEmpty(void);
boolean isFull(void);

int top = -1;

struct stack 
{
	calloc ( sizeof(int) );
}
